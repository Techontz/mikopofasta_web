import { Loading } from "@/components/ui/Loading";

/** Shown while the next page loads, so navigating covers the screen with the loader the live system uses. */
export default function AppLoading() {
  return <Loading />;
}
